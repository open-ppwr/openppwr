export function reconcile({sourceCount=0,accepted=0,rejected=0,duplicate=0,pending=0,dlq=0,sourceTotal=0,targetTotal=0}={}){const variance=Number((targetTotal-sourceTotal).toFixed(6));return {sourceCount,accepted,rejected,duplicate,pending,dlq,reconciled:accepted-rejected-dlq,variance,status:variance===0&&sourceCount===accepted+rejected+duplicate+pending?'ZGODNE':'WYMAGA_WYJAŚNIENIA',message:variance===0?'Dane uzgodnione.':'Występują różnice wymagające wyjaśnienia.'};}
